import styled from "styled-components";

export const Top = () => {
  return (
    <SContainer>
      <h2>Index page</h2>
    </SContainer>
  );
};

const SContainer = styled.div`
  text-align: center;
`;
